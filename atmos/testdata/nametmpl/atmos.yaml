base_path: ""

components:
  terraform:
    base_path: "components/terraform"

stacks:
  base_path: "stacks"
  name_template: "{{.vars.account}}__{{.vars.resource_name}}"
  included_paths:
    - "aws/**/*"
  excluded_paths:
    - "**/_defaults.yaml"
